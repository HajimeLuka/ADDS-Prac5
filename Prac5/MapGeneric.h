#ifndef MAPGENERIC_H
#define MAPGENERIC_H

#include <iostream>
#include <stdio.h>
#include <math.h>
#include <vector>

using namespace std;


class MapGeneric{
    public:
    MapGeneric();
    vector<int>;
    map(vector<int>);

    private:
    virtual int f(int) = 0;
};


#endif